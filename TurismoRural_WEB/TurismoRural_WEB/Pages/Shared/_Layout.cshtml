                    @{
                        var usuarioLogueado = Context.Session.GetInt32("UsuarioId") != null;
                    }

                    @if (usuarioLogueado)
                    {
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-page="/Reservations/Create">
                                    Agregar Reserva
                                </a>
                            </li>
                        </ul>
                    }